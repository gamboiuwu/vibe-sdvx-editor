{\rtf1\ansi\ansicpg1252\cocoartf2867
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\froman\fcharset0 Times-Roman;}
{\colortbl;\red255\green255\blue255;\red0\green0\blue0;}
{\*\expandedcolortbl;;\cssrgb\c0\c0\c0;}
\margl1440\margr1440\vieww11520\viewh8400\viewkind0
\deftab720
\pard\pardeftab720\sa240\partightenfactor0

\f0\fs24 \cf0 \expnd0\expndtw0\kerning0
[v0.0.19 SHIPPED] Practice Playback Rate: Slider in the Game Preview side panel (0.25\'d72.0\'d7) that slows or accelerates both audio and chart tick advancement together. Rate label turns amber when not at 1.0\'d7. TapeStop FX fixed to ramp from the active rate instead of hardcoded 1.0. Saved via Save Config.\
\
[v0.0.20 SHIPPED] Chart Section Labels: Named ranges (Intro, Verse, Chorus, Bridge, Outro) overlaid as colored bands on the 2D ruler. Section Navigator panel lists all sections with click-to-seek. Selection-aware Add button uses the active selection range or defaults to 4 measures at playhead. Color swatch cycles through 7-color palette. Stored as _sections KSON custom field. Also fixed CHANGELOG syntax error from v0.0.19 that caused a SyntaxError at startup.\
\
[v0.0.21 SHIPPED] Section-Relative Paste: Right-click context menu includes a "Paste at Section..." submenu listing all named chart sections. Selecting a section pastes clipboard at that section's start tick with full undo support.\
\
[v0.0.22 SHIPPED] Per-Note FX Effects + Chromatic Aberration Glitch: Each FX hold now independently stores its own effect type and parameter values. "None" option added to silence lane-chain effects per hold. Glitch Events now produce SVG-based chromatic aberration (per-channel RGB offset filter) and CSS keyframe frame distortion, both scaling with glitch level. FX hold labels display per-note effect type.\
\
[v0.0.23 SHIPPED] Pattern Snippet Library: Floating panel (Window -> Pattern Snippets...) for saving and reusing named note patterns. Save any selection, name it, and it persists in localStorage. Click to load into clipboard and paste with Ctrl+V. Right-click context menu includes "Save as Snippet..." option. Double-click to rename snippets. Filter/search bar for large libraries.\
\
[v0.0.24 SHIPPED] KSM-Style Laser Rendering: 2D editor lasers now match the KSM Editor / SDVX in-game appearance. Anchor dots no longer draw during normal viewing or playback - they appear only when the laser tool is active, the section is being edited, an anchor is selected, or the "Show envelope points always" preference is enabled. Ribbon half-width retuned from 0.425*BT_W to 0.38*BT_W to match KSM Editor proportions. The always-visible outline stroke was removed (it now only renders on the section being edited), giving the flat-color KSM ribbon look. A thin SDVX-style inner highlight stripe was added along the ribbon center for the characteristic laser glow. Fixes the long-standing bug where anchor dots were drawn unconditionally regardless of the showLaserDots flag.\
\
=== UPDATES ===\
\
2026-05-22: Verified v0.0.22 (Per-Note FX + Chromatic Aberration Glitch) and v0.0.23 (Pattern Snippet Library) integration. Both features are intact: Pattern Snippets panel is wired through Window menu and right-click context menu; per-note FX overrides survive in chart data and FX hold labels reflect the per-note effect type; SVG feColorMatrix filter for chromatic aberration is correctly applied to the game preview during glitch events. No regressions found in app.js / renderer.js startup paths.\
\
2026-05-22: Fixed laser rendering to match KSM Editor and SDVX in-game appearance. Root-cause was that the anchor-dot drawing block in renderer.js _drawColLasers had no outer guard checking showLaserDots, so dots were always drawn even during playback or when other tools were active. Wrapped the block with a drawDots gate that respects showLaserDots, the user preference, active-section state, and selected-point state. Also removed the always-visible 0.8px outline stroke that gave ribbons an "outlined" rather than KSM-flat look, retuned ribbon half-width, and added an SDVX inner-highlight stripe. Shipped as v0.0.24 on the Experimental fork.\
\
Point 1 (BPM Calibration Panel)\
\pard\pardeftab720\sa240\partightenfactor0
Calibration Mode should include a dedicated BPM analysis and BPM entry section that works alongside waveform-based timing alignment. Right now calibration focuses only on offset and start point alignment, but BPM is equally important because it defines how musical time is structured once the chart is synced.\
Inside Calibration Mode, there should be a clearly separated BPM panel that allows both manual BPM entry and automatic BPM detection. This panel is not part of the main waveform interaction area. It is a supporting control section that updates the timing model used by the beat grid and preview playback system.\
Manual BPM entry should be a simple input field where the user can directly type a BPM value. When this value is changed, the system must immediately update the beat grid spacing across the waveform and preview systems. This includes recalculating measure divisions, beat intervals, and timing markers in real time without requiring a reload or recalibration reset. The BPM value becomes part of the active calibration state and directly affects how musical time is interpreted across the entire chart.\
Alongside manual entry, there should be an automatic BPM calibration function that is triggered by holding or pressing the spacebar while in a dedicated BPM detection mode. When activated, the system plays the audio while analyzing rhythmic spacing patterns in the waveform. It detects recurring transient intervals such as kick drums, snare patterns, or consistent beat spacing, and uses this data to estimate the approximate BPM of the song.\
Importantly, during this BPM detection playback, the calibration cursor must remain visually stable in terms of position tracking behavior. The system should continue to show playback progress through the waveform, but it should not modify or shift the calibration marker or offset during detection. The purpose of this mode is purely analytical, not structural. It listens and measures, but does not alter timing alignment while it is running.\
Once detection completes or is manually stopped, the system should present an estimated BPM value to the user. This value should be shown as a suggestion rather than an automatic overwrite. The user must confirm whether they want to apply it to the calibration state. If confirmed, the BPM grid, waveform beat overlay, and playback timing system should update immediately to reflect the new BPM.\
The BPM detection system should be treated as a helper tool rather than an authoritative source. It provides an estimate based on audio rhythm patterns, but the final BPM decision always belongs to the user because rhythm games often include intentional BPM changes, swing timing, or irregular structures that automated detection cannot fully interpret correctly.\
This BPM calibration layer must integrate directly with the existing KSON timing system so that any BPM applied here is stored as part of the chart\'92s metadata and can be used later by the preview engine to correctly compute playback speed, lane movement, and beat synchronization. It ensures that Calibration Mode is not only responsible for aligning the start point of the chart but also for establishing the fundamental tempo structure that the rest of the editor relies on.\
Status: PARTIALLY DONE (v0.0.18 added Tap Tempo and Auto-Detect; Spacebar-triggered detection mode not yet implemented)\
\pard\pardeftab720\sa240\partightenfactor0
\

Point 2 (Orthographic Mode Overhaul)\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 The orthographic preview mode must behave like a strict 2D projection of the Sound Voltex lane system, not a pseudo-3D or perspective-influenced rendering. Right now the issue is that elements such as BT buttons, FX lanes, and lasers are not correctly constrained to a flat plane, causing them to visually \'93stick out\'94 or appear incorrectly layered. In true SDVX orthographic view, everything exists on a single 2D gameplay plane where depth is not visually simulated. The lane itself must move strictly in a linear downward direction with uniform speed mapping, exactly matching beat progression. There must be no perspective convergence, no vanishing point distortion, and no simulated depth stretching. Official SDVX gameplay still uses perspective in gameplay mode, but orthographic/editor style views in tools and references behave as flat timing representations for clarity and accuracy.\
The BT buttons must be fully embedded into the lane plane. They are not floating objects and must not visually protrude or have depth offset. Each BT input should behave like a 2D hit object that only changes state when it intersects the judgment line at the bottom. When active, BT buttons should only visually \'93light up\'94 at the exact moment they cross the judgment line. They should not animate independently in space or appear offset from the lane surface. FX buttons follow the same rule and must remain strictly 2D overlays aligned to their lane positions.\
Lasers (VOL-L and VOL-R) must also be fully flattened into the same 2D orthographic plane during edit and orthographic preview mode. They must not appear as segmented 3D ribbons or floating curves. Instead, they must be rendered as continuous smoothed spline paths with no visible control points, no anchor dots, and no intermediate construction geometry during playback or non-edit states. The system must explicitly hide all pen tool handles, anchor nodes, and intermediate control points unless the laser is actively selected in edit mode. This mirrors professional vector editing behavior.\
Status: NOT YET STARTED\
\

Point 3 (Chart Section Label System)\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 Status: SHIPPED in v0.0.20. Named chart sections with start/end ticks, labels, and colors are overlaid on the 2D ruler. Section Navigator panel (Window menu) for one-click seek. Stored in KSON as _sections custom extension field.\
\

Point 4 (New Recommended: Per-Section FX Automation Lane)\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 The FX chain currently supports adding effects to FX-L and FX-R lanes, but the effect parameters (retrigger length, gate frequency, flanger rate, etc.) cannot be automated over time. The editor should add an automation lane view similar to how REAPER or Ableton handle automation: each FX effect parameter gets a thin lane below the main chart lane, with a drawn curve or step sequence representing the parameter value over time. Users should be able to draw, drag, and edit automation breakpoints directly on the chart canvas. The automation data would be stored in KSON as part of the audio.fx.ch arrays or as a new nested automation key. This matches the system description\'92s requirement for "REAPER FX style automation or effect lanes".\
\

Point 5 (Section-Relative Paste Operations)\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 Status: SHIPPED in v0.0.21. Right-click context menu now includes a \'93Paste at Section\'85\'94 submenu that dynamically lists all named chart sections. Selecting a section entry pastes the clipboard at that section\'92s start tick with full undo support.\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 \
\
Point 6 (New Recommended: Orthographic Mode Overhaul)\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 Status: NOT YET STARTED. The orthographic (2D editor) preview mode must be a strict flat 2D projection with no perspective influence. BT/FX buttons must be fully embedded into the lane plane without depth offset, and lasers must render as continuous smoothed spline paths with no visible construction geometry. See Point 2 for full specification.\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 \
\
Point 7 (New Recommended: Per-Section FX Automation Lane)\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 The FX chain supports adding effects to FX-L and FX-R lanes but effect parameters (retrigger length, gate frequency, flanger rate, etc.) cannot be automated over time. The editor should add an automation lane view similar to REAPER or Ableton: each FX effect parameter gets a thin lane below the main chart lane with a drawn curve or step sequence representing the parameter value over time. Users can draw, drag, and edit automation breakpoints directly on the chart canvas. Automation data would be stored in KSON as part of the audio.fx.ch arrays or as a new nested automation key. This matches the system description\'92s \'93REAPER FX style automation or effect lanes\'94 requirement.\
Status: NOT YET STARTED\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 \
\
Point 8 (New Recommended: Waveform Preview Strip in 2D Edit View)\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \strokec2 The 2D chart editor currently shows only notes, lasers, and grid lines. A valuable addition would be a thin waveform strip rendered along the bottom or left edge of the edit canvas that shows the audio waveform amplitude in sync with the chart timeline. This would allow chartists to visually align notes to audio transients without switching to the Calibration window. The strip should be toggleable, semi-transparent, and use the same tick coordinate system as the rest of the editor so it scrolls in lockstep with the chart. The waveform data would be derived from the already-loaded audioBuffer using offline peak analysis, cached as a compact array of peak pairs, and redrawn only when the visible column range changes. This is a read-only visual aid and does not modify any chart data or timing model.\
Status: NOT YET STARTED\
\pard\pardeftab720\sa240\partightenfactor0
\cf0 \outl0\strokewidth0 \
}
